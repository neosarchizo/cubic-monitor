import {RouteMap} from '../../routes/types'

const routeMap: RouteMap = {
  ROUTE_MAIN: '/main',
  ROUTE_TABLE: '/table',
  ROUTE_CHART: '/chart',
  ROUTE_EXPORT: '/export',
}

export default routeMap
